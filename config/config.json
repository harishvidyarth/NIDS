{
  "dirs": {
    "pcaps": "pcaps",
    "features": "features",
    "results": "results"
  },
  "extraction": {
    "_comment": "Feature extraction uses the cicflowmeter PyPI package in-process (backend/extraction/extract.py). Run scripts/patch_cicflowmeter.py once after installing requirements, see README.md."
  },
  "capture": {
    "wireshark_dir": "C:/Program Files/Wireshark"
  },
  "verdict_gate": {
    "_comment": "Confidence gate for the headline traffic verdict (backend/prediction/predict.py::summarize_states). An attack class reads red ATTACK only when its OWN share of scored flows reaches min_ratio for its tier (or the deterministic signature layer confirmed it); otherwise amber SUSPICIOUS. Purely percentage-based, no absolute flow count. Edit and restart the backend to retune.",
    "ddos": { "min_ratio": 0.20 },
    "other": { "min_ratio": 0.20 }
  }
}
