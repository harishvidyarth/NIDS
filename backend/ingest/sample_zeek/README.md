# Synthetic Zeek JSON sample

`sample.log` contains 200 JSON objects: 130 ordinary connections, a 20-event
periodic beacon, 30 DNS records including an NXDOMAIN tunnel pattern, and 20
TLS records containing both baseline and novel fingerprints. Addresses are
documentation/private ranges and no Zeek installation is required.
